claas paleta{
  PVector pos;
  int ancho;
  int alto;
  PVector vel = new PVector(0,0);
  boolean tecla arriba?
  boolean tecla abajo?
  pelota(float columa){
    pos = new PVector(columna, height/2);
  }
  void mostrar(){
    fill(255);
    rect(CENTER);
    rect(pos.x, pos.y, ancho, alto);
  }
